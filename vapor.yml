id: 8706
name: curbside
environments:
    production:
        domain: curbside.run
        database: vapor
        memory: 1024
        cli-memory: 512
        build:
            - 'composer install --no-dev'
            - 'php artisan event:cache'
            - 'npm ci && npm run prod && rm -rf node_modules'
